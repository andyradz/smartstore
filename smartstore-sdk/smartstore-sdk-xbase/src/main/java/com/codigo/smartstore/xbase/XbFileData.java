package com.codigo.smartstore.xbase;

// Klasa reprzentuje strukture z danymi zbioru XbBase
public class XbFileData {

	// open connection ..może zrobić to na zleceniu
	// close connection ..może zrobić to na zleceniu
	// new record dodaj nowy rekord
	// usun rekord
	// aktualizuj rekord
	// lista wierszy
	// lista kolumn
	// iterator danych
	// watch file service czy plik istnieje na dysku
	// Database - baza danych powinna być zapisana na dysku ze swoim stanem(
	// tj.tabele, obiekty nazwa, itp)
	// .get('kartoteki') //jest tworzone połaczenie z danymi
	// .use('FILENAME'); dostajemy tabele z danymi
	// .read() dostajemy iterator
	// .read(page,ind1,count)
	// .save() //zapisuje wiersz, zlecenia transakcje
	// .rows().create() dostajemy wiersz danych
	// .save(newRow) //lokuje zbiór, zatwierdza commita, unlock
	// Database
	// .lock(table1);
	// .unlock(table1);
	// .commit(table1);
	// .uncommit(table1);
	// .close(table1);
	// Database
	// .get('kartoteki')
	// .getIndex('')
}
